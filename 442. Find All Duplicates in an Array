class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        c = collections.Counter(nums)
        res = []
        for i in c:
            if c[i] >=2:
                res.append(i)
        return res
