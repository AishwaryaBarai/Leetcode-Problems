class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        sum = 0
        product = 1
        for i in str(n):
            product = int(product * int(i))
            sum = int(sum + int(i))
        return (product - sum)
        
