class Solution:
    def rob(self, nums: List[int]) -> int:
        prev1,prev2 = 0,0
        for i in nums:
            cur = max(prev1,i+prev2)
            prev2 = prev1
            prev1 = cur

        return prev1
