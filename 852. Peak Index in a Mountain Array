class Solution:
    def peakIndexInMountainArray(self, A: List[int]) -> int:
        if len(A) >=3:
            i = 0
            while i < len(A):
                if A[i+1] < A[i]:
                    return i
                i+=1
        
