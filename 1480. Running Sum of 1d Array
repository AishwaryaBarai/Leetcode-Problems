class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        if nums:
            lst = [0] * len(nums)
            lst[0] = nums[0]
            for i in range(1,len(nums)):
                lst[i] = lst[i-1] + nums[i]
            return lst
