class Solution:
    def moveZeroes(self, nums: List[int]) -> None:
        j = 0
        l = len(nums)
        for i in nums:
            if i !=0:
                nums[j] = i
                j+=1
        for k in range(j,l):
            nums[k] = 0
