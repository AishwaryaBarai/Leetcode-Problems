class Solution:
    def repeatedNTimes(self, A: List[int]) -> int:
        arr = []
        for i in A:
            if i not in arr:
                arr.append(i)
            else:
                return i
            
